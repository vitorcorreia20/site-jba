# Banco de dados (Vercel Postgres / Neon) — a Vercel preenche isso
# automaticamente ao conectar um banco Postgres ao projeto.
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""

# NextAuth
# Gere um valor com: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Banco de dados (Vercel Postgres / Neon) — a Vercel preenche isso
# automaticamente ao conectar um banco Postgres ao projeto.
POSTGRES_PRISMA_URL=""
POSTGRES_URL_NON_POOLING=""

# Vercel Blob — upload direto de imagens
# Crie em vercel.com > Storage > Create > Blob e conecte ao projeto; copie o token
BLOB_READ_WRITE_TOKEN=""
BLOB_STORE_ID=""

# NextAuth
# Gere um valor com: openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"
